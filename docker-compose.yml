version: "3.9"

services:
  backend:
    build: .
    container_name: sheinpulse_api
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build:
      context: ./frontend
    container_name: sheinpulse_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend